server:
  port: 8000
  servlet:
    context-path: /

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/humandb
    driver-class-name: org.mariadb.jdbc.Driver
    username: human
    password: human1234
  jpa:
    open-in-view: false
    hibernate:
      ddl-auto: none
      
    properties:
      '[hibernate.format_sql]': true    
  output:
    ansi:
      enabled: always

logging:
  level:
    '[org.hibernate.SQL]': DEBUG
    #root: DEBUG
