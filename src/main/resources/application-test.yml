server:
  port: 8001
  servlet:
    context-path: /
    encoding:
      charset: utf-8

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/humandbtest
    driver-class-name: org.mariadb.jdbc.Driver
    username: human
    password: human1234

  output:
    ansi:
      enabled: always

logging:
  level:
    '[org.springframework.web]': DEBUG
